/** @type {import("stylelint").Config} */
const stylelintConfig = {
  // Includes the kebab-case rule for class names (selector-class-pattern).
  extends: ["stylelint-config-standard"],
};

export default stylelintConfig;
